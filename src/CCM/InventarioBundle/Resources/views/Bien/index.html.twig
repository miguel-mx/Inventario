{% extends 'InventarioBundle::layout.html.twig' %}
{% block titulo %}
    <i class="fa fa-laptop"></i> Bienes
{% endblock %}


{% block body -%}

    <table class="table table-striped table-bordered table-hover" id="dataTables-example">
        <thead>
            <tr>
                <th>Id</th>
               {# <th>Noinvetario</th>
                <th>Foliosicop</th>
                <th>Fechaadq</th>#}
                <th>Descripcion</th>
                {#<th>Marca</th>
                <th>Modelo</th>
                <th>Serie</th>#}
                <th>Comentario</th>
                <th>Ubicacion</th>
                <th>Validofis</th>
                <th>Tipoadq</th>
                <th>Estatus</th>
                <th>Foto</th>
                {#<th>Created</th>
                <th>Modified</th>#}
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('bien_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
            {#    <td>{{ entity.noInventario }}</td>
                <td>{{ entity.folioSicop }}</td>
                <td>{% if entity.fechaAdq %}{{ entity.fechaAdq|date('Y-m-d H:i:s') }}{% endif %}</td>#}
                <td>{{ entity.descripcion }}</td>
                {#<td>{{ entity.marca }}</td>
                <td>{{ entity.modelo }}</td>
                <td>{{ entity.serie }}</td>#}
                <td>{{ entity.comentario }}</td>
                <td>{{ entity.ubicacion }}</td>
                <td>{% if entity.validoFis %} Si {% else %} No {% endif %}</td>
                <td>{{ entity.tipoAdq }}</td>
                <td>{% if entity.estatus %} Activo {% else %} Inactivo {% endif %}</td>
                <td>{{ entity.foto }}</td>{#
                <td>{% if entity.created %}{{ entity.created|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{% if entity.modified %}{{ entity.modified|date('Y-m-d H:i:s') }}{% endif %}</td>#}
                <td>
                <ul>
                    <li>
                        <a href="{{ path('bien_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('bien_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('bien_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
